<?xml version="1.0" encoding="utf-8"?>


<LinearLayout
        android:orientation="vertical"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="true"
        android:focusableInTouchMode="true"
        >

    <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            >

        <ImageView
                android:id="@+id/text_focus_bg"
                android:src="@drawable/usercenter_indicator_focused"
                android:visibility="invisible"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                />

        <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:gravity="center"
                android:layout_gravity="center"
                >

            <TextView
                    android:id="@+id/indicator_text"
                    android:textColor="#FFFFFF"
                    android:gravity="center"
                    android:textSize="@dimen/usercenter_fragment_indicator_item_text_size"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    />

            <ImageView
                    android:id="@+id/text_select_bg"
                    android:src="@drawable/indicator_image"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:scaleType="fitXY"
                    android:visibility="invisible"
                    />


        </LinearLayout>
    </FrameLayout>

    <ImageView
            android:src="@drawable/usercenter_indicator_deivider"
            android:layout_width="match_parent"
            android:scaleType="fitXY"
            android:layout_height="wrap_content"/>

</LinearLayout>
